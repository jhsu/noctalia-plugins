# Launcher Notifications

Launcher provider for Noctalia Shell that lists recent notifications using:

```bash
qs -c noctalia-shell ipc call notifications getHistory
```

Use `>notif` in the launcher to browse and filter notification history.
